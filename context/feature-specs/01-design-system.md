Read `AGENTS.md` before starting.

We are adding the disgn system an UI primitive components.

Install and cofigure `shadcn/ui`.

Add these shadcn components:
 - Button
 - Card
 - Dialog
 - Input
 - Tabs
 - Textarea
 - ScrollArea

Do not modify the generated `components/ui/*` files after installation.

Also Install `lucid-react`.

Create 'lib/utils.ts' with reusable `cn()` helper for merging Tailwind classes. 

Ensure all the components match the existing dark theme in `gobals.css`. 


### Check when done
 - All components import without errors
 - `cn()` works properly
 - No default light styling appears 

